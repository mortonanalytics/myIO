# Distribution fitting transform

Fits a parametric distribution to univariate data via MLE using base R.
Returns a grid of x values and scaled density values suitable for
overlaying on a histogram.

## Usage

``` r
transform_fit_distribution(data, mapping, options = list())
```
