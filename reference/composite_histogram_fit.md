# Histogram + fitted distribution composite expansion

Expands into a histogram layer plus a fitted density line. Optionally
includes a text annotation with distribution parameters.

## Usage

``` r
composite_histogram_fit(data, mapping, label, color, options)
```
