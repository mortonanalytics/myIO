% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_duckdb_wasm.R
\name{install_duckdb_wasm}
\alias{install_duckdb_wasm}
\title{Install the DuckDB-WASM binary for large-dataset virtualization}
\usage{
install_duckdb_wasm(
  version = NULL,
  from = NULL,
  force = FALSE,
  quiet = !interactive()
)
}
\arguments{
\item{version}{Character scalar naming the version to install. Defaults
to the latest row in `inst/duckdb-wasm-manifest.csv`.}

\item{from}{Optional local directory containing pre-downloaded binaries
(`duckdb-mvp.wasm` and `duckdb-browser-mvp.worker.js`). Airgap path.}

\item{force}{Logical. If TRUE, overwrite existing cached binaries.}

\item{quiet}{Logical. Suppress progress output. Defaults to
`!interactive()`.}
}
\value{
Invisibly returns the cache path where the binary was installed.
}
\description{
Downloads and verifies the DuckDB-WASM runtime into a user-local cache
so the in-browser big-data engine is available for subsequent widgets.
This follows the keras3 / torch / reticulate pattern: the binary is
NOT bundled with the R package and is not required for small-data use.
}
\examples{
\donttest{
# Downloads ~22 MB from the upstream mirror into the user cache.
install_duckdb_wasm()
}
}
